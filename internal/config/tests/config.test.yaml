general:
  port: 8080

metrics:
  port: 9090

database:
  path: "test.db"

tracker:
  url: https://test-domain.atlassian.net/rest/api/3/issue
  token: test-jira-api-token

ticket:
  project:
    key: TEST
  issuetype:
    name: Task
  labels:
    - Test
    - Critical
  priority:
    name: High
